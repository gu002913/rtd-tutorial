[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "panda-compliance"
authors = [{name = "dingyi-gu", email = "gu002913@outlook.com"}]
dynamic = ["version", "description"]
